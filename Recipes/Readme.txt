These folders contain snippets for log files for both Linux and Windows. For Windows only, also event viewer entries that can be added.

These recipes can grow to large numbers of files without bloating the default configuration.
